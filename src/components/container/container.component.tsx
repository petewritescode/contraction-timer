import React, { FunctionComponent } from 'react';
import { StyledContainer } from './container.styles';

export const Container: FunctionComponent = ({ children }) => <StyledContainer>{children}</StyledContainer>;
