export interface Theme {
    color: {
        background: string;
        text: string;
        control: {
            border: string;
            icon: string;
            start: string;
            stop: string;
        };
    };
}
