export interface Theme {
    color: {
        background: {
            stopped: string;
            contraction: string;
            rest: string;
        };
        text: string;
        control: string;
    };
}
